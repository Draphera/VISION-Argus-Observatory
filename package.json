{
  "name": "vision-argus-observatory",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "build": "node scripts/build-static.js",
    "guard:public": "node scripts/public-export-guard.js",
    "prepublish:check": "npm run guard:public && npm run build"
  }
}
